#include "vars.h"

// Initialize the global screenoffset variable
vector2D<float> screenOffset(0.0, 0.0);

// init the level size vector
vector2D<float> levelSize;

// This is initialized by game
assets *zbeAssets;
